
import React from 'react';

// AdminMenu component is no longer needed since we're moving all admin links to the secondary nav
const AdminMenu: React.FC<{ isAdmin: boolean }> = () => {
  // Return null since we're not using this component anymore
  return null;
};

export default AdminMenu;
